#ifndef SOUND_EFFECTS_H
#define SOUND_EFFECTS_H

// Sound effect IDs
typedef enum {
    SOUND_CATCH = 0,
    SOUND_DOOR = 1,
    SOUND_FOOTSTEP = 2,
    SOUND_MENU = 3,
    // Add more as needed
    SOUND_COUNT
} SoundID;

#endif // SOUND_EFFECTS_H